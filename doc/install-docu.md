# install npm
pom.xml -> frontend-maven-plugin

# install angular cli
```bash
@echo off
SET PATH=%PATH%;%cd%\node\;%cd%\
node .\node_modules\@angular\cli\bin\ng %*
```

# install cli itself??
```bash
@echo off
SET PATH=%PATH%;%cd%\node\;%cd%\
node .\node_modules\@angular\cli\bin\ng %*
```